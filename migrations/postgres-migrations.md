### Uses [node-pg-migrate](https://github.com/theoephraim/node-pg-migrate#column-definitions)

### Handle migrations

Run `yarn pg-migrate up`.

### Create a new migration

Run `yarn pg-migrate create [name]`.

import { CISource } from "danger/distribution/ci_source/ci_source"

export const source: CISource = {
  isCI: true,
  isPR: true,
  name: "Peril",
  pullRequestID: "not used",
  repoSlug: "not used",
}
